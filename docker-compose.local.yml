version: '3'
services:
  postgres-dev:
    image: postgres:11.2
    environment:
      - POSTGRES_DB=weather
      - POSTGRES_HOST=localhost
      - POSTGRES_USER=dev
      - POSTGRES_PASSWORD=
    ports:
      - "5432:5432"
